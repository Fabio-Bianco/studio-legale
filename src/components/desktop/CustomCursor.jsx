// src/components/desktop/CustomCursor.jsx
import React from 'react';

/**
 * Custom Cursor Component - Simple Gold Cursor
 * Now just applies CSS cursor styling, no complex logic needed
 */
export default function CustomCursor() {
  // The cursor is now handled entirely by CSS
  // This component exists only for consistency but doesn't render anything
  return null;
}